# Nixpacks configuration for Safari Backend on Railway
# Railway Root Directory: complete_backend/backend

[phases.setup]
nixPkgs = ["nodejs_18", "python3"]

[phases.install]
cmds = ["npm ci --production"]

[start]
cmd = "node src/app.js"
